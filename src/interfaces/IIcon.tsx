export interface IconData {
  icon: string;
  alt: string;
  label?: string;        // para tecnologías u otros iconos que usan label
  "en-label"?: string;   // para habilidades y secciones multilenguaje
  "es-label"?: string;   // para habilidades y secciones multilenguaje
  link?: string;
  plus?: string;
}